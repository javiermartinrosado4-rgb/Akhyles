import React from "react";

/**
 * The kintsugi launch animation is a native-only handoff from the splash
 * screen. The web build keeps the same layout/API version without rendering
 * a mobile splash sequence.
 */
export function KintsugiIntroPreview(_props: { replay?: boolean }) {
  return null;
}
